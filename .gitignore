# Directories/files that may be generated by this project
dist
# Only ignore build artifacts at repo root (do not ignore dependencies/build — that is the scoped runtime tree).
/build
/build-module
/build-style
/build-types
node_modules
chunks
includes/blocks
includes/json
# Only ignore Composer vendor at repo root; dependencies/vendor is regenerated with `cd dependencies && composer install`.
/vendor
dependencies/vendor/
scoped-vendor
php-scoper.phar

# Directories/files that may appear in your environment
.DS_Store
*.log
phpcs.xml
yarn.lock

.cache
*.tsbuildinfo
.docker/mysql/*
package-lock.json
.vscode/launch.json
CLAUDE.md
*.md

# Test artifacts
tests/e2e/.auth/
playwright-report/
test-results/
coverage/